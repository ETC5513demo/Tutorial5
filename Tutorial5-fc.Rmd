---
title: "Learning to rebase"
author: "Fan"
date: "`r Sys.Date()`"
output:
  bookdown::html_document2
---

# Introduction

This is how we learn to rebase a git repository.

# COVID-19 visualizations

```{r message=FALSE}
library(tidyverse)
library(COVID19)
```

```{r cache=TRUE, message=FALSE}
# Exam situations in USA
covid_us <- covid19("USA", level = 2, verbose = FALSE)
```

```{r, warning=FALSE, fig.align='center'}
# head(covid_us)
ggplot(covid_us, aes(x = date, y = confirmed)) + 
  geom_line() + 
  facet_wrap(~id)
```

```{r}
covid_us %>% 
  dplyr::filter(id %in% unique(covid_us$id)[1:5]) %>% 
  ggplot(aes(x = date, y = confirmed)) + 
  geom_line() + 
  facet_wrap(~id)
```




